# NL2BR-PASS — builder brief (2026-07-23, morning)

STATUS: READY FOR BUILD. Branch off current `main`, one PR. Cold audit: same-day, post-merge — a chosen gate ordering this time (see WORKFLOW-TRACE T7/T10). **Merging to `main` publishes the live demo** (`/demo` serves this repo's `main` via GitHub Pages rewrite), so squash-merge only when the checklist below is green.

## Objective

Make `nl2br()` in `builds/chrome/player.js` honor the line breaks the module data intends. Root cause (BUILD-LESSONS, 07-21): the pattern matches a literal backslash-n two-character sequence, but `MODULE` data carries real newlines — so every heading's intended break silently word-wraps. This is now a one-file fix, which was the point of consolidating first.

## Scope

`courses/loto/builds/chrome/player.js` only. No CSS, no shells, no data, no menu.

## Recipe

1. Fix the conversion to turn real newlines (`\n`) in heading text into `<br>`. Check whether other fields flow through the same helper; enumerate every render path that calls it in the change note and confirm intended behavior per field.
2. Full-deck heading verification: for every heading across M1–M3, compare rendered breaks against the newlines present in `MODULE` data. Every `\n` in data → a rendered `<br>`; no visual change where data has no newline.

## Hard constraints

- Word-multiset per module unchanged (line breaks add no words).
- Only heading line-wrapping may change. Screenshots: every heading-bearing slide × 3 modules × both modes, before/after; every diff must map to a newline in data — any other diff is a FAIL.
- Screenshots committed to the branch only; strip with a cleanup commit before squash-merge (convention: WORKFLOW-TRACE, gate-artifacts item).
- After merge: GitHub Pages rebuilds in ~1–2 min; spot-check one fixed heading at the live `/demo` URL.

## Handback

- `courses/loto/CHANGE-NOTE-nl2br.md` — the pattern before/after, every field flowing through the helper, and the per-heading verification table (module · slide · newlines in data · breaks rendered).
- Branch screenshots as above.

## Audit checklist (cold, post-merge; baseline = this pass's merge-base)

- **N1** — diff confined to `chrome/player.js` + change note (screenshots stripped before merge).
- **N2** — per-heading table complete and re-derivable from `MODULE` data; independently spot-verify 6 headings (2 per module).
- **N3** — word-multiset × 3 unchanged vs baseline.
- **N4** — no rendering change outside heading line-wrapping (screenshot review of the branch matrix).
- **N5** — the helper's other consumers (if any) behave exactly as the change note claims.
