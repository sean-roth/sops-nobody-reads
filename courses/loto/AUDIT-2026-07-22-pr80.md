# Cold Post-Merge Audit — PR #80 (chrome-consolidation)

**Date:** 2026-07-22
**Subject:** Merge commit `888bebd` (PR #80, branch `chrome-consolidation`, tip `10bb150`) vs merge-base `a61bfe0` (pre-PR main)
**Checklist:** §Audit in `courses/loto/CHROME-CONSOLIDATION-2026-07-21.md`
**Ground rules:** Cold audit — no project context beyond the brief, the PR, and the named commits. `CHANGE-NOTE-chrome-consolidation.md` treated as claims to verify, not as evidence. Read-only: no remediation attempted.

## Verdict: PASS

All seven checklist items pass. All three extra-attention items pass. No FAIL was encountered, so the stop-and-report condition never triggered.

## Preliminaries — merge topology

- `888bebd` is a merge of `d21cba1` (main) and `10bb150` (PR tip, a single commit). `git merge-base` of the parents is `a61bfe0` exactly, confirming the instructed comparison base.
- Main-side commits during the PR window (`fb62218`, `d21cba1`) touched documentation only. The only difference between `10bb150` and `888bebd` under `courses/loto/` is `DECISIONS-NEEDED.md` (a main-side edit, out of PR scope) — i.e., the PR's `builds/` and `screenshots/` content landed in the merge byte-unaltered.
- `courses/loto/` is identical between `888bebd` and the main tip at audit time (`d8ea006`): the audited state is what is live.

## Checklist results

### A1 — module JSON byte-identical to main — PASS

Method: `git ls-tree` blob hashes at `a61bfe0` vs `888bebd` for `builds/module-01.json`, `module-02.json`, `module-03.json`.
Result: all three hashes identical → byte-identical.

### A2 — rendered-text word-multiset identical per module — PASS

Method: headless Chromium (puppeteer) full walks of both sides, capturing `document.body.innerText` at every state — each slide, each quiz question unanswered and answered, and the close screen — in both Learner and Reviewer modes. Word multisets (sorted token lists) hashed per module.
Result: pre == post per module — m01: 3,929 words; m02: 3,643; m03: 4,875 — SHA1-equal in each case. (The change note's counts of 1,334/1,213/1,759 reflect a narrower capture scope; equality of the multiset is the checklist criterion and holds under this broader capture.)

### A3 — chrome in exactly one place — PASS

Method: full read of all three shells plus greps for `<style`, `style=`, `function`, `addEventListener`, `querySelector`, `getElementById`, `document.` across the shell files.
Result: each shell contains exactly — head + `<title>`, two relative CSS links, `<div id="app">`, an inline `MODULE` block **byte-identical** to the old inline data, a 9-line `MODULE_CHROME` block, and the `player.js` script reference. Zero style rules, zero logic. `MODULE_CHROME` is the change-note-listed exception (judgment call 1), so this is within the documented boundary.

### A4 — relative chrome references; file:// and static server both work — PASS

Method: verified all shell references are `../chrome/…` relative; ran full click-throughs over `file://` and over `http://` (python `http.server`, separate port per side): both sides × 3 modules × 4 toggle combos (plain, Reviewer, Dark, Dark+Reviewer).
Result: every walk booted, navigated all slides, answered all quiz questions with visible feedback, reached close, and back-navigated to slide 1 with no page errors. The menu (`builds/index.html`) is untouched by the PR (empty diff) and uses relative module links. One symmetric console 403 appeared on every load, both sides: the Google Fonts `@import` blocked by the audit sandbox's egress proxy — an environment artifact identical on both sides, not a PR defect.

### A5 — screenshot matrix complete; every pixel diff explained — PASS

Method A (committed matrix): enumerated `screenshots/chrome-consolidation/` — 132 PNGs, zero filename-pattern violations, 66 complete before/after pairs. Coverage cross-checked against each module's JSON: distinct slide types + {quiz, quiz-answered, close} = 11/10/12 containers × light/dark × before/after = 132 exactly. All 66 pairs are byte-identical (sha256) → zero pixel diffs → nothing requiring explanation.
Method B (independent render): because byte-identity alone cannot distinguish honest deterministic renders from copied files, the full 66-state matrix was independently re-rendered from `a61bfe0` and `888bebd` in headless Chromium (1280×800). Result: 65/66 pairs byte-identical; the single nonzero pair (`m03-teaching-caption-light`, 232,249 px) was investigated and attributed to the audit harness's own capture racing the 1.1 MB background JPEG: with explicit background-image decode before capture, pre and post are pixel-identical on 3/3 repeated captures and each side is self-consistent at 0 diff. No rendering difference exists at that state.

### A6 — nl2br headings identical (bug intact) — PASS

Method: code diff plus runtime inspection.
Result: the `nl2br` line is byte-identical old→new — `replace(/\\n/g, '<br>')` matches a literal backslash-n, so it remains a no-op on real newlines. At runtime, headings containing raw `\n` (11/8/13 across m01/m02/m03) render with zero `<br>` elements, and heading `innerHTML` is identical pre vs post. The bug is preserved, unfixed, exactly as scoped.

### A7 — divergence table accounts for every difference — PASS

Method: independent three-way comparison of the old players at `a61bfe0`, enumerated before consulting the table.
Result: body DOM byte-identical across all three; heads differ only by `<title>`. Comment-stripped CSS is SHA1-identical across all three (zero rule differences); the comment-level diffs are exactly D1 (seqRows) and D2 (nl2br note). Renderers differ on exactly three lines per pair — 237 (quiz feedbacks = D3), 260 (next link = D4), 262 (close note = D5). Nothing exists outside the table; every table row corresponds to a real difference.

## Extra-attention items

### E1 — screenshot count — PASS

Counted independently: 132 (66 states × before/after), with the container derivation matching each module's JSON. See A5.

### E2 — behavior-path changes: injected nav DOM and `#app { height: 100% }` — PASS

- `PLAYER_MARKUP` in `player.js` is byte-equal (after indent-strip) to the old static body DOM; `mountChrome()` is invoked before element lookups/wiring.
- Runtime: `#app`'s first child is `#player`; all expected element ids present; menu link href correct.
- Heights: `#app` clientHeight = 800 (viewport), `.player` 800, `.slide` 739, bottom-bar top 739 — equal to pre in every module × light/dark × Learner/Reviewer combination. The rule does its documented job (prevents the blank-slide collapse) and introduces no layout deviation.
- Static cross-check: comment-stripped `tokens.css + chrome.css` differs from the old stylesheet by **exactly one line**: `#app { height: 100%; }` — the "only genuinely new rule" claim is precisely true.
- Full click-throughs: forward through all slides and quiz to close, back to slide 1, both toggles exercised with label/aria-pressed/class assertions, both modes, both transports.

### E3 — MODULE_CHROME strings verbatim vs old renderers — PASS

Method: extracted `feedbacks`, `nextHref`, `nextLabel`, `closeNote` from each shell's `MODULE_CHROME` and the corresponding literals from each old renderer at `a61bfe0`; compared as evaluated JS values.
Result: 12/12 equal (3 modules × 4 fields). Runtime corroboration: close-screen `innerHTML` is byte-equal pre vs post in every walk.

## Method notes and limitations

- Google Fonts are unreachable from the audit sandbox (egress proxy). Both sides fell back to the same system fonts, so all pre-vs-post comparisons are fair; absolute pixel output may differ from a font-connected environment, which does not affect any checklist criterion.
- The committed matrix's byte-identical pairs are consistent with either honest deterministic renders or file copying; the independent re-render (A5, Method B) resolves this in favor of the former at the render level.
- Audit performed read-only. No remediation was needed or attempted; remedy on any FAIL was designated the orchestrator's call, and no FAIL occurred.

— Cold auditor (Claude), 2026-07-22
