# LOTO Course — Adversarial Re-Audit, Round 3 (post Pass-7)

**Date:** 2026-07-15
**Auditor:** fresh-context Claude (Fable) — no context from production or from the fix passes, per Durability Standard §9 (auditor ≠ fixer)
**Audit target:** `main` @ `6af6b58` (merge of PR #71 / Pass-7 commit `8e0ef70`)
**Ground truth:** `courses/loto/sources/29-CFR-1910.147.md` (incl. Appendix A) and `courses/loto/sources/29-CFR-1910.399-qualified-person.md` — **only** these. Live eCFR/OSHA.gov not consulted for any claim judgment.
**Mandate:** find and report. Nothing was fixed. This file is the sole output.

**Scope (buyer/learner-openable):** all three scripts; all three build JSONs; all three players including hardcoded JS quiz-feedback strings; `README.md`; `DECISIONS-NEEDED.md`; `sources/PROVENANCE.md`; all three image manifests; the hub `builds/index.html`; `BRIEF.md`; shipped image assets (paths and reference parity). **Excluded:** `AUDIT-*.md`, `LOTO-AUDIT-*.md`, `PRODUCTION-LOG.md`, `_quarantine/`.

---

## GATES

**Staleness episode, on the record:** the first run of this audit stopped before Pass 0 — `main` HEAD was `1f245d1` (the round-2 audit commit itself) and Pass-7 existed only as unmerged branch commit `8e0ef70`; main still taught the round-2 blocker. Halted per the brief's staleness rule; Sean merged (PR #71); audit re-ran from scratch against post-merge main. → META 1.

**Topological gate:** `git merge-base --is-ancestor 8e0ef70 main` → PASS. Pass-7 is an ancestor of the audit target.

**Precondition grep:** `"five type" | "four role" | "Five types of hazardous energy"`, case-insensitive, across `builds/`, `scripts/`, `README.md` → **zero hits.** PASS.

---

## PASS 0 — Source readiness

- **Checksums:** recomputed SHA-256 of both frozen files; both match `PROVENANCE.md` exactly (`28f449dd…2cf157`; `20a58560…540f86`). PASS.
- **Authority sweep:** every citation in every deliverable layer names 1910.147 (incl. Appendix A), 1910.399, or the 1910.331/1910.332 excerpts that live *inside* the frozen 1910.399 file. Grep for NFPA / ANSI / Z244 / NIOSH / any other 1910.x across scripts, JSONs, players, hub, README: zero real hits (the only matches are the letters "ansi" inside the word "TRANSITION" in panel notes). **Zero unfrozen authorities.** PASS.
- **Statistics sweep:** zero fatality/injury statistics anywhere. "OSHA requires" appears exactly once in the package — as M1 quiz Q4's option A, a marked-wrong distractor (accepted in prior audits; unchanged). PASS.
- **Source-native typos confirmed still intact in the frozen file** (relevant to anchor byte-exactness): "perfomed" at (c)(6)(i)(A); "Verfication" at (e)(3)(i).

### NOTE 0.1 — Subpart-O disposition still unrecorded (carried; 4th audit)

The minor-servicing verbatim anchor (M3 script L201 / build slide[13]) faithfully carries the source's "(See subpart O of this part)" cross-reference. Subpart O is not a frozen source and `DECISIONS-NEEDED.md` still contains no §2.4 disposition for the reference (accept-as-inert, freeze, or annotate). Round-2's 4.1 asked for this entry; items 1–3 were updated, this wasn't. Carried.

---

## PASS 1 — Source fidelity, claim by claim

**Mechanical anchor verification (byte-level, whole-anchor first, then segment-wise for ` / ` composites with locator-label prefixes stripped):**

- **Rendered chips: 21** (M1: 6 slide + 1 quiz; M2: 2 slide; M3: 11 slide + 1 quiz; +2 over round-2's 19, both from Pass-7 — the (e)(2)(ii) split anchor and the affected-becomes-authorized anchor). **All 21 verify EXACT** against the frozen file each names, including every composite segment (M1 Q3 ×2; M2 s12 ×2; M3 s7 ×2, s20 ×2, s23 ×3). **Zero unverifiable chips. Zero whitespace-normalized chips.** The M3 role anchors preserve the source's own triple-space definition separators byte-for-byte.
- **Script-layer source_refs: 19** (M1: 7; M2: 2; M3: 10). All verify, **but not all byte-exact** → 1.1.

### FRICTION 1.1 — Script-layer anchor grammar: unfixed, and Pass-7 extended the pattern (carries 07-14 R2 1.4; **fourth consecutive audit**)

1. **Two M1 script anchors are whitespace-normalized, not character-exact:** L91 and L340 quote `"Energy source. Any source of…"` (single space); the frozen file reads `Energy source.   Any source` (triple space, source-native). Durability Standard §1 defines the quote field as "the verbatim source text, **character-exact**" — these fail the standard's own spec. The builds sidestep it by starting their anchors at "Any source of…" (byte-exact), so script and build anchors also diverge.
2. **Three script anchors carry embedded editorial commentary inside the anchor value:** M1 Appendix-A ref (L177, `-- NOTE: Appendix A has no per-step id anchors…`); M2 (c)(2) ref (L179, `-- two separate numbered paragraphs…`); and **Pass-7's new closing-notification anchor (M3 L86)**, which appends two sentences of rationale and a DECISIONS-NEEDED pointer. The commentary is useful and the pointer is good practice — but it lives inside the field defined as verbatim source text. The corresponding build anchors strip it, so the layers diverge in exactly the way round-2 described. The fix pass that resolved the anchor's *content* re-used the flawed *grammar*.
3. **The composite ` / ` convention remains undocumented.** Checked `docs/standards/DURABILITY-STANDARD.md` in full: §1 specifies character-exactness but no composite grammar, no commentary rule. Verification tooling still cannot distinguish "composite by convention" from "corrupted anchor" without hand-written splitting logic (this audit had to write it).

Same recommendation, verbatim from two audits ago, still open: one verbatim span per anchor; multi-provision content carries an array of sources; commentary goes in a sibling field; spec it in the standard. → META 4.

### FRICTION 1.2 — M2 script end card drops the "by default" hedge on tags (new)

`scripts/module-02-…md` L345: **"Tags supplement locks but never replace them"** — no hedge. This contradicts the frozen two-case structure the same script teaches correctly 160 lines earlier (L177–181): under (c)(2)(i) a non-lockable device's tagout doesn't "supplement" any lock — it *is* the system; under (c)(2)(ii) tagout *can* replace lockout where full employee protection per (c)(3) is demonstrated. The script's own body line is "They don't replace them **by default**" (L181), and the build summary (`module-02.json` slide[22], last item) carries the hedge: "…never replace them **by default**." So the learner-visible layer is correct and the layers diverge — with the over-claim sitting in the regeneration upstream. A future rebuild "matching the script's own treatment" mints this into the learner-visible summary; that promotion mechanism is exactly how round-2's 1.5 interpretation reached slide[26]. Small fix: three words in one script line.

### NOTE 1.3 — Unanchored conservative imperatives persist; still no §8 additions ledger (carries R2 1.5)

Unchanged inventory, all conservative-direction, all still unlogged (the course has no additions-ledger artifact at all, which §8 requires for exactly this content class): "document the attempts" and "Before they approach any equipment" (M3 script L254/L256 — both also learner-visible in build slide[18], where they predate Pass-7) vs (e)(3)(ii)–(iii)'s narrower text; "Every machine with hazardous energy should have one" (M2 L55) with the (c)(4)(i) Note documentation exception still untaught; the notification content list (M2 L66 / slide[5]). The periodic-inspection "**watches**… perform it" gloss remains learner-visible (build slide[29]; script L350) — (c)(6)(i)(A)/(C) require an uninvolved inspector and a *review* of responsibilities, not observation of a performance.

### NOTE 1.4 — "This standard defines three roles" over-claim persists (carries R2 1.6)

Script L282; build slide[20] label "Three Roles — 29 CFR 1910.147(b)" over "Authorized. Affected. Other." — (b) defines two of the three; "other employees" is a (c)(7)(i)(C) training class. The slide's anchor still carries the honest version (it quotes only the two (b) definitions).

### NOTE 1.5 — "OSHA's Own Words" kicker persists (carries R2 1.7)

`module-01.json` slide[18]: "Capacitors. Springs. Suspended loads. Trapped pressure." — capacitors and springs are Appendix A step (6)'s words; "suspended loads" and "trapped pressure" are the course's translations of "elevated machine members" and "air, gas, steam, or water pressure."

### Verified clean (search shown, not vibes)

Six named sources + "other," in the source's own order, in every learner-visible layer (M1 slides[7],[31], quiz Q3; M3 slide[34]; both summary cards; README) ↔ (b) — grep for "five" across the package returns only slide[7]'s deliberate "not five" rebuttal (→ 3.4). Gravity framed as not-a-source in both layers ("Not a Seventh Source"), anchor verbatim ↔ Appendix A step (6). Three roles, never four; qualified-person reframe fenced, both 1910.399 chips byte-exact; the gloss "trained to work on or near exposed energized electrical parts" traces to the frozen 1910.331(a)/1910.332(b)(3) excerpts, not to memory. **(e)(3) trigger is unavailability — not "emergency" — in every learner-visible layer**; the only textual "emergency" is the absent worker's *reason for leaving* (M3 L241), which is correct usage (the filename residue → 3.5). Master-key material carries its "not a regulation's phrase — a design principle" disclaimer in script (L155) and build (slide[10]) alike. Cord-and-plug: both conditions, conjunctively, and **R2 FRICTION 1.3 is verifiably fixed** — "your sight" is gone; the replacement gloss ("not set down anywhere someone else could reach it") runs the conservative direction, per §6's hard gate. **R2 FRICTION 1.2 is verifiably fixed** — the not-lockable case now teaches "its own set of requirements: more rigorous periodic inspections, additional training" (↔ Appendix A General + (c)(6)(i)(D) + (c)(7)(ii)) in script L177 and build slide[12]. Minor servicing: all five conditions, conjunctive ("All of it. Every time."), misuse named, anchor verbatim. Testing/positioning: all five (f)(1) actions, script keeps them distinct, build compressed-but-ordered (→ 3.2). Reaccumulation taught and chip-verbatim ↔ (d)(5)(ii). Return-to-neutral ↔ Appendix A step (7) Caution (M2 slide[15]) and restoration step (3) (M3 step 4). Contractor information-exchange-not-certificate ↔ (f)(2)(i), anchored. All 12 quiz keys verified against the scripts (M1 B,C,D,B; M2 B,C,B,C; M3 B,C,C,B). 2000 PSI remains scenario color, never attributed to the regulation. No 50-pound tag-attachment figure taught (defensible scope call, unchanged).

---

## PASS 2 — Layer consistency, both directions, and order

**Parity:** slide counts M1 32/32, M2 23/23, M3 36/36; quizzes 4/4/4. **Each player's inline `MODULE` block is deep-equal to its disk JSON** — full structural comparison after tolerating the M2/M3 trailing commas (→ 2.2). No fabricated slides, no dropped slides, in any build. **Image parity is exact in all three modules:** every JSON-referenced image ships; nothing shipped is unreferenced (M1 10/10, M2 7/7, M3 8/8).

**Sequence — verified in every layer (script, JSON, player):**
- **(d)(1)–(6) application order:** M1 preview (slide[26]) and M2's full walk teach Prepare → Shutdown → Isolate → Apply devices → Stored energy → Verify, one-to-one with the source, in order. The M2 tags digression sits between steps 4 and 5 in both script and build — a digression, not a resequencing.
- **Restoration order — the round-2 blocker's ground:** nine steps, and the critical ordering is now …Remove your lock → **Notify (devices removed)** → **Re-energize** → start/use → **Notify (ready for use)** — in the script (L45–92), the JSON (slide[3] list; slides[5]→[6]→[7]), and the player (deep-equal). Slide[6]'s boxText makes the ordering load-bearing: "Only after that notice goes out." Notification precedes re-energization, which precedes start — **stricter than (e)(2)(ii)'s before-start mandate, in the conservative direction.** The before-start notice is anchored to (e)(2)(ii) alone; the closing notice to (c)(9) + Appendix A step (5). **R2 BLOCKER 1.1 is dead in every layer** — searched all layers for any after-startup notification ordering: none remains.
- **(f)(1) testing/positioning order:** script lists all five actions distinctly, in source order; build compresses to four phrases but preserves order (→ 3.2).

**Verified fixed:** R2 FRICTION 2.1 — the dangling "that's step five" pointer is gone, fixed at the root: Pass-7 removed the pre-removal notice (an unsourced course invention) from the script rather than building a referent for it. R2 FRICTION 2.2 — affected-becomes-authorized restored to the build as slide[26] with a byte-exact (b) anchor, alongside the surviving script line (L315).

### NOTE 2.1 — Player-only quiz-feedback layer persists (carries R2 2.3)

All three players carry 12 learner-readable feedback strings existing in neither script nor JSON. **All 12 re-verified content-accurate against the frozen sources this round** — including M3 Q4's, which fully implements the script's two-count distractor note (this standard doesn't define "qualified person"; where defined, it doesn't mean "experienced with the equipment"). Still invisible to script↔JSON parity tooling; same recommendation: promote feedback into the JSON schema.

### NOTE 2.2 — M2/M3 inline data still valid-JS-not-strict-JSON (carries R2 2.4)

Trailing comma after the last slide in both; M1 strict. This audit's first strict-JSON extraction false-failed both, as predicted. Content-equal once tolerated.

### NOTE 2.3 — Trivial script→build drops: two carried, one new (carries R2 2.5)

Carried: "Who to contact" (M2 notify list) and "Then inform the people in the area…" (M2 step six). New this round: **"The coordinator reviews the lockout procedure with everyone before work begins"** (M3 script L112) has no build counterpart (slide[8] says only "manages the process"). All three are unanchored elaborations — the claim itself isn't in the frozen source either ((f)(3)(ii)(B) is *ascertain exposure status*, not *review the procedure*) — so the drops arguably improve fidelity, but under the script-is-upstream convention divergence should be deliberate. Fold into the 1.3 disposition.

---

## PASS 3 — Structural coherence

### FRICTION 3.1 — Citation coverage still inversely correlated with operational weight (carries R2 3.1, again marginally improved)

Improved: the restoration sequence now carries two chips (slides[5],[7]). Still zero citations on M3's group lockout ((f)(3)), lockbox ((f)(3)(ii)(D) — the source says "group lockbox" verbatim; the chip writes itself), shift overlap ((f)(4)), and the nine-step list slide itself; M2's steps one through four and six remain chip-less while the tags digression and reaccumulation are cited. Same buyer-facing asymmetry.

### FRICTION 3.2 — Course-invented list still out-emphasizes the source's mandated sequence (carries R2 3.2)

The restoration list — a course composite of Appendix A's five restoration steps plus course additions — is now **nine** steps with a full numbered-list slide. The (f)(1) sequence the source says "shall be followed," in order, remains compressed to four prose phrases on slide[15] under a boxText claiming "An exact sequence, in order" — tools and personnel merged into one phrase (all five actions survive; the script's VISUAL keeps five distinct). The blocker that used to live inside the nine-step list is gone; the emphasis inversion isn't.

### NOTE 3.3 — M2 Q4 feedback still blanket (carries R2 3.3)

"The procedure applies regardless of job duration" — correct (duration is not the minor-servicing test, and the Q4 scenario fails the exception's conditions anyway), consistent with M3's fence, but still no pointer to the exception M3 teaches. One clause closes it.

### NOTE 3.4 — "Six named sources, not five" artifact now fully orphaned (carries R2 3.4)

`module-01.json` slide[7]. With the README fixed, the package no longer contains the error this line rebuts — it is now the *only* "five" in the course, arguing with a ghost. To a fresh learner it reads as a non sequitur.

### NOTE 3.5 — Manifest and asset residue

Pass-7's manifest update chose **honest disclosure over renumbering**: the "Emergency Lock Removal" row is relabeled to the unavailability framing ✓; the slide-number column is explicitly declared unreliable with `module-03.json` named as ground truth ✓; the 33→36 history is accurate (verified against git) ✓. Residue that remains: (a) **the shipped filename `builds/module-03/img/s11-emergency-removal.jpg` still carries the dead "emergency" framing** — invisible to learners (players render images, not names), visible to any buyer browsing the package; (b) the M1 manifest's "Do not trust `IMAGE-PROMPTS.md`" warning now points at a file Pass-7 moved to `_quarantine/` — the warning is still true, the referent moved. Cross-module contradiction sweep: none found (M2's lock-removal forward-reference ↔ M3's (e)(3) teaching consistent; M2 Q4's duration ruling ↔ M3's minor-servicing fence consistent — duration is not the test in either).

---

## PASS 4 — Scope & decisions

**Folded-in exclusions:** all conditions present, conjunctive, fences intact, anchors byte-exact — including the repaired cord-and-plug gloss. PASS.

**DECISIONS-NEEDED ↔ shipped state: MATCHES.** Item 1 is closed as folded-in with an accurate history of its own staleness (R2 FRICTION 4.1 verifiably fixed). Item 2 open, nothing shipped depends on it. **Item 3 records precisely what shipped:** the two-notification split, the positions, and the anchor assignments in the decision record match the script, JSON, and player exactly (verified span-by-span). **Item 3 is applied but pending Sean's sign-off — noted here, not resolved here.** The shipped ordering satisfies the mandatory (e)(2)(ii) under any reading of the reenergize/start distinction (the notice precedes both), so the pending item is governance, not a content defect.

### FRICTION 4.1 — The hub markets "ensure compliance with OSHA standards" (new)

`builds/index.html`, first paragraph a buyer reads: "Learn the procedures that protect lives, prevent injuries, and **ensure compliance with OSHA standards**." No generic course can ensure compliance with 1910.147: (c)(7)(i)(A) training is workplace-specific by its own text ("the energy available **in the workplace**"); tag-limitations training remains partial ((c)(7)(ii)(B),(C),(D),(F) untaught — original state, per R2's git verification); compliance is a program property (procedures + training + periodic inspections, (c)(1)), not a course property. This is the strong version of exactly the positioning question NOTE 4.2 has carried open for three audits — asserted on the package's front door while the decision record contains no ruling. A compliance-literate buyer reads this sentence *before* opening a single module. Top friction to fix before any demo; the fix is copy ("built on," "training aligned to," "supports your compliance program"), not content.

### NOTE 4.2 — Positioning decision still not on the record (carries R2 4.2)

Unchanged: fine as an awareness course; a defect in a compliance-training claim; no DECISIONS-NEEDED entry exists. 4.1 above is what happens while it stays undecided.

**Open signer decisions surfaced (not resolved):** item 3 sign-off (notification-ordering ruling); item 2 (interpretation letters); subpart-O disposition (0.1); positioning ruling + hub copy alignment (4.1/4.2).

---

## PASS 5 — Citation feature integrity

- **Chip verification: PASS.** 21/21 rendered chips byte-exact against the frozen files (see Pass 1 for the census). Zero unverifiable chips.
- **Toggle mechanics: PASS.** `let reviewerMode = false` — defaults to Learner in all three players; single in-memory boolean, nothing persisted; button carries `aria-pressed`; `renderCitation()` returns `''` unless reviewer mode is on and both `citation` and `url` exist; invoked on both slide and quiz render paths.
- **Storage: PASS.** Zero `localStorage`/`sessionStorage`/`indexedDB`/cookie **calls** in any shipped HTML; the only occurrences are explanatory comments naming what was avoided (permitted by this audit's terms).
- **SCORM posture:** `window.API` remains a graceful-degradation stub — computes and displays, never reports (carries 07-14 5.3; for the scorm-builder pass). Consistent with the README's unchecked "SCORM Packaging" box.
- **R2 NOTE 5.2 (ground-truth epistemics) verifiably fixed:** the README now states that every taught claim traces to the frozen, checksummed regulation text and points at `PROVENANCE.md` and the Durability Standard; the phantom OSHCON source line is gone.

### NOTE 5.1 — Fail-safe still doesn't guard `anchor` (carries R2 5.1)

`renderCitation` requires `citation` and `url` but not the verification key. Moot today — all 21 sources carry anchors — still one condition from permanently closed.

---

## GUARD G1 — Regression check against prior verified-present inventories

**The brief's four named items — all present:**
1. **Both notifications:** before-application (M2 slide[5], script step one) ✓; devices-removed, before start (M3 slide[5], anchored (e)(2)(ii)) ✓; plus the closing ready-for-use notice (M3 slide[7]) ✓.
2. **(c)(6) periodic inspection:** M3 slide[29] + summary item 8 + script L350 — annual cadence ✓, uninvolved-inspector independence ✓.
3. **Return-to-neutral:** M2 slide[15] (application-side, Appendix A step (7) Caution) ✓; M3 restoration step 4 (Appendix A restoration step (3)) ✓; both scripts ✓.
4. **Affected-becomes-authorized:** script L315 + build slide[26] with byte-exact (b) anchor ✓.

**07-11 WHAT CHECKED OUT, re-walked item by item:** six-step sequence ✓; one-person-one-lock, each affixes/removes own ✓; lockbox ✓; group coordinator ✓; shift overlap ✓; tags-as-warnings ✓; tag-limitations — identically partial (original state, → 4.2); annual inspection by uninvolved authorized employee ✓ — and "certification contents" was git-verified against the oldest script blob: the paperwork section has *never* enumerated (c)(6)(ii)'s contents; the 07-11 annotation was to the provision, not course text — **no regression** (same resolution method as R2's "names, dates" check); training certification ✓; notifications ✓; release sequence ✓; affected-becomes-authorized ✓ (now in every layer).

**Both 07-14 verified-clean lists re-checked:** every item present and correct (master-key disclaimer, qualified-person fence + both 1910.399 chips, reaccumulation, cord-and-plug both-conditions, minor-servicing five conditions, contractor info-exchange, cold-open architecture, no 50-PSI corruption).

**Pass-7 introduced no losses.** Its full diff was reviewed line-by-line: additive/surgical. The two removals are correct-by-design: the pre-removal notice (unsourced course invention; removing it killed R2 2.1's dangling referent at the root) and the "you don't re-energize before replacing guards" ordering phrase — whose substance survives as step 3's own text ("Every guard goes back before startup"). The re-energize teaching displaced from slide[4] reappears intact on new slide[6].

## GUARD G2 — Completeness, claim-level, script → JSON

Every taught claim traced into its JSON, all three modules — at claim level, not section-header level, per R2 META 2. **Gaps found: exactly the three trivial unanchored-elaboration drops filed at 2.3** ("Who to contact"; "inform the people in the area"; coordinator-reviews-the-procedure). Everything else — every energy source, every step, every fence condition, every notification, every quiz stem, option, and key, both scripts' summary cards — is present in the JSON, and JSON=player deep-equality carries it to the learner. The player-only feedback layer (2.1) remains the one learner-visible surface outside this trace; audited separately and clean.

---

## VERDICT

**Is this course defensible in front of a compliance-literate buyer today: YES.**

Zero blockers. The round-2 blocker is dead in every layer — the mandatory (e)(2)(ii) notice now demonstrably precedes re-energization, which precedes start, and the within-source conflict that produced it is recorded as a §5 decision item with the shipped resolution documented accurately. Every prior blocker across all three audit rounds is verified resolved; every prior fix re-verified intact; the fix pass introduced no losses.

**Stated caveats, so the YES is honest:**
1. **DECISIONS-NEEDED item 3 awaits Sean's sign-off.** The notification-ordering resolution shipped in Pass-7 is content-correct under any reading (the notice precedes both re-energize and start), so this is governance debt, not a content defect — but the record says "pending," and it should be signed or revisited before the package is represented as decision-complete.
2. **Top frictions before a demo:** the hub's "ensure compliance with OSHA standards" claim (4.1 — copy fix, five minutes, and the single likeliest sentence to cost credibility with exactly the buyer this course targets); the M2 script end-card's unhedged tags line (1.2 — three words); and the script-anchor grammar (1.1 — now four audits old, and the fix pass itself keeps re-using the flawed pattern).
3. Everything else is carried friction and notes, individually filed above, none buyer-blocking.

---

## META — for the course-audit skill

1. **Add a topological gate to the brief template.** This run's premise ("post Pass-7") was false of the audit target — Pass-7 sat unmerged, and main still taught the round-2 blocker. String preconditions only catch known-bad phrases; they cannot catch "the fix never landed." New standing gate, before Pass 0: *confirm the fix-pass commit/PR named in the premise is an ancestor of the audit target* (`git merge-base --is-ancestor`). Cost: one command. This run is the existence proof.
2. **Buyer-facing marketing copy is a content surface no pass owns.** The hub's "ensure compliance" claim (4.1) is not a taught claim (Pass 1), a layer divergence (Pass 2), or a decision-log mismatch as currently defined (Pass 4). R2 META 7 named the sibling gap for the README's self-description; this round confirms the class. Name the check: *every buyer-facing surface claims only what the decision record supports* — and put the hub inside every sweep's path scope.
3. **Grep shipped asset *filenames* against retired-framing strings, not just file contents.** `s11-emergency-removal.jpg` survived three rounds of "emergency" eradication because every sweep read contents. Filenames ship.
4. **The anchor-grammar spec belongs in the Durability Standard, not in per-course findings.** Four consecutive audits of the same FRICTION, and the newest fix pass re-used the flawed pattern while fixing the anchor's content — per-course flagging demonstrably does not converge. §1 already says character-exact; add: one verbatim span per anchor, arrays for multi-provision content, commentary in a sibling field, and a byte-level verifier as the acceptance check.
5. **Checks invented this run, worth keeping:** (a) embedded-MODULE extraction must be trailing-comma-tolerant (strict JSON parsing false-fails M2/M3, exactly as R2 NOTE 2.4 predicted — deep-equal after tolerant parse is the right parity test); (b) name the G1 sub-step that has now resolved two would-be regressions ("certification contents" this round, "names, dates" last round): *before filing a G1 loss, git-verify the baseline item was course text rather than a provision annotation in the old audit's margin.*
