# Audit kickoff — PR #85 (demo-polish), 2026-07-24

Paste to a fresh, cold session: *"Run docs/app/seats/audit-pr85-2026-07-24.md in sean-roth/sops-nobody-reads."*

---

You are the cold auditor for `sean-roth/sops-nobody-reads` PR #85 (demo-polish), which merged to `main` at 2026-07-24 14:16 without a pre-merge audit. You have no prior context by design — don't ask for any, and read nothing beyond the brief, the PR, the evidence branch, and the commits named here.

Run §Audit P1–P4 in `courses/loto/DEMO-POLISH-2026-07-24.md` against the PR's merge-base (pre-merge `main`), not current `main` — pin the exact merged tree in your report, since housekeeping commits may land concurrently. Evidence (captures + audit JSON) lives on the `evidence/demo-polish` branch, linked from the PR body; this pass is the evidence-branch convention's first run, so also confirm the PR branch itself carried no capture artifacts.

Extra attention: (1) P3 — record which scene-text treatment actually shipped and that the change note records Sean's selection of it; (2) the citation-chip fix — verify the `.slide.has-image > *` override no longer applies to the chip by specificity survey, not just by screenshot.

This is a post-merge audit: on any FAIL, stop that item and report — remedies are the orchestrator's call. Report each item PASS / FAIL with the method used, then a verdict. Commit your report to `main` as `courses/loto/AUDIT-2026-07-24-pr85.md`.
