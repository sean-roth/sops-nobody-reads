# WORKFLOW-TRACE — record → spec for app v0 — T27 addendum F (2026-07-24)

Fold into the master trace with addendums A–E (and the builder's T25) at the reflection
session.

- **T27 — Audit governance ratified; the self-authorship conflict becomes a constitutional
  rule.** Auditor (Opus, drafting) → orchestrator (non-author review) → human (adoption) ·
  PRs sops-nobody-reads#86 + snr-app#1, both stopped at handback by the drafting seat ·
  Sean's commission ("point any Claude at a course audit") which the seat correctly
  reframed against the actual gaps · **merged on Sean's go** (`e0e61fb`, `89f5283`):
  course-audit-SPEC v1.1 (lane router; **Pass 6 render conformance R1–R6** — nothing in
  v1.0 would have caught #85's defect, so a course could pass all six passes while shipping
  visually broken; four earned render tells; open-decision-is-a-BLOCKER in Pass 4; the
  COURSE-AUDIT- rename, **accepted forward-only** — the shared namespace already produced
  the hallucinated 07-13 file) and auditor seat v2 (S1 sourcing-not-ignorance replacing the
  unenforceable coldness rule; S2 context-provenance header; S3 tree-pinning; S4 tool
  paths; S5 verdict grammar with non-flattened findings; S6 META close), plus the FINDINGS
  amendments (coldness-as-sourcing principle; cross-seat context bleed pre-registered at
  n=0).
  **Rulings:** (1) rename accepted, forward-only; (2) Pass 6 provisional until an inaugural
  run against LOTO, executed by a **non-author session on a different model** (a fresh
  Fable, not Opus) — scheduled post-calls, not demo-blocking; (3) the self-authorship
  conflict resolved by a standing **Ratification** rule added to the seat doc on merge:
  drafting is open, ratifying requires a non-author seat plus Sean, inaugural execution is
  cross-authored, and the rule binds every seat symmetrically — orchestrator included
  (precedent T25).
  *App notes: (1) separation of powers is now explicit: propose / review / adopt are three
  different seats, and the runner should encode rule-change PRs as a distinct gate type
  requiring a non-author reviewer. (2) A drafting seat that reframes its commission against
  the record — and names its own conflict of interest unprompted — is the brief-is-a-
  hypothesis principle applied to governance itself. (3) "An audit that overstates its
  independence is worse than one that discloses a qualified independence, because the first
  is unfalsifiable" — adopt as spec language for every self-reporting component.*
